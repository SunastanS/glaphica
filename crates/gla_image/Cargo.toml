[package]
name = "gla_image"
version = "0.1.0"
edition = "2024"

[dependencies]
atlas = { path = "../atlas" }
glaphica_core = { package = "core", path = "../core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
