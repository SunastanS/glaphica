[package]
name = "gla_renderer"
version = "0.1.0"
edition = "2024"

[dependencies]
atlas = { path = "../atlas" }
gla_color = { path = "../gla_color" }
gla_core = { path = "../gla_core" }
bytemuck = { version = "1", features = ["derive"] }
wgpu = "28.0.0"

[dev-dependencies]
pollster = "0.4.0"
tile_key = { path = "../tile_key" }
