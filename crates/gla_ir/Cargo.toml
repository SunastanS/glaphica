[package]
name = "gla_ir"
version = "0.1.0"
edition = "2024"

[dependencies]
gla_color = { path = "../gla_color" }
gla_command_core = { path = "../gla_command_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
