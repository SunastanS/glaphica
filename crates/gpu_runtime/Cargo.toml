[package]
name = "gpu_runtime"
version = "0.1.0"
edition = "2024"

[features]
default = []
blocking = ["dep:pollster"]

[dependencies]
atlas = { path = "../atlas" }
glaphica_constants = { path = "../glaphica_constants" }

wgpu = "28.0.0"
pollster = { version = "0.4.0", optional = true }
